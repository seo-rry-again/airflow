SELECT
    source_id,
    area_code,
    area_name,
    temperature,
    sensible_temperature,
    max_temperature,
    min_temperature,
    humidity,
    wind_direction,
    wind_speed,
    precipitation,
    precipitation_type,
    precipitation_message,
    sunrise,
    sunset,
    uv_index_level,
    uv_index_desc,
    uv_message,
    pm25_index,
    pm25_value,
    pm10_index,
    pm10_value,
    air_quality_index,
    air_quality_value,
    air_quality_main,
    air_quality_message,
    observed_at,
    created_at
FROM
    {{ source("raw", "weather") }}
